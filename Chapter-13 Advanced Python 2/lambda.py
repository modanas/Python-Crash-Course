# Lambda functions are small anonymous functions that can be used in place of normal functions.
# They are defined using the lambda keyword.


square = lambda x: x * x * x
print(square(5))

sum = lambda x,y,z: x+y+z
print(sum(1,2,3))